{
  "name": "@foxyselabs/schemas",
  "version": "0.1.0",
  "private": true,
  "description": "Shared JSON Schema definitions (TypeScript + Python).",
  "scripts": {
    "build": "echo 'no build step'"
  }
}
